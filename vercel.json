{
  "version": 2,
  "name": "blogs",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "pnpm install --force && npx tsc"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "src/index.ts" }]
}