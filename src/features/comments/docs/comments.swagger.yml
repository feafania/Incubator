openapi: 3.0.0
info:
  title: Comments API Documentation
  version: 1.0.0

tags:
  - name: Comments
    description: API for managing comments

paths:
  /comments/{id}:
    get:
      summary: Get comment by id
      tags: [Comments]
      parameters:
        - in: path
          name: id
          required: true
          description: Id of existing comment
          schema:
            type: string
      responses:
        200:
          description: Success — comment found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommentViewModel"
        404:
          description: Not Found

  /comments/{commentId}:
    put:
      summary: Update existing comment by id
      tags: [Comments]
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: commentId
          required: true
          description: Comment id to update
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CommentUpdateInput"
      responses:
        204:
          description: No Content — comment updated successfully
        400:
          description: Invalid input model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorListOutput"
        401:
          description: Unauthorized
        403:
          description: Forbidden — trying to edit someone else’s comment
        404:
          description: Comment not found

    delete:
      summary: Delete comment by id
      tags: [Comments]
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: commentId
          required: true
          description: Comment id
          schema:
            type: string
      responses:
        204:
          description: No Content — comment deleted successfully
        401:
          description: Unauthorized
        403:
          description: Forbidden — trying to delete someone else’s comment
        404:
          description: Comment not found

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    CommentUpdateInput:
      type: object
      required:
        - content
      properties:
        content:
          type: string
          example: "Updated comment text"

    CommentatorInfo:
      type: object
      required:
        - userId
        - userLogin
      properties:
        userId:
          type: string
          example: "a1b2c3d4"
        userLogin:
          type: string
          example: "sensei"

    CommentViewModel:
      type: object
      required:
        - id
        - content
        - commentatorInfo
        - createdAt
      properties:
        id:
          type: string
          example: "507f1f77bcf86cd799439011"
        content:
          type: string
          example: "This is a sample comment"
        commentatorInfo:
          $ref: "#/components/schemas/CommentatorInfo"
        createdAt:
          type: string
          format: date-time
          example: "2025-11-03T21:26:35.347Z"

    ValidationErrorListOutput:
      type: object
      required:
        - errorsMessages
      properties:
        errorsMessages:
          type: array
          items:
            type: object
            required:
              - message
              - field
            properties:
              message:
                type: string
                example: "Invalid content length"
              field:
                type: string
                example: "content"
